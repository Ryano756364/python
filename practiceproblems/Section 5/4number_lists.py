even = [2, 4, 6, 8]
odd = [1, 3, 5, 7, 9]

# min, max are functions
print(min(even))  # 2
print(max(even))  # 8
print(min(odd))  # 1
print(max(odd))  # 9

print()
print(len(even))  # 4
print(len(odd))  # 5

print()
print("Mississippi".count("s"))

# when we call a method, we tell it which object it's called on
# s.append(x)
# appending x to a list called s
# we pass x as an argument, so that the method knows what to append
# OOP is big with . notation
# if method needs argument, you put it in the parenthesis

